#jdbc.driver=com.mysql.cj.jdbc.Driver
#jdbc.url=jdbc:mysql://127.0.0.1:3306/scoula_db
jdbc.driver=net.sf.log4jdbc.sql.jdbcapi.DriverSpy
jdbc.url=jdbc:log4jdbc:mysql://localhost:3306/scoula_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
jdbc.username=scoula
jdbc.password=1234
gemini.api.key=${GEMINI_API_KEY}

# 비밀번호 찾기 인증번호 메일 발송 (Gmail SMTP)
# MAIL_USERNAME : 보내는 Gmail 주소
# MAIL_PASSWORD : Gmail 2단계 인증을 켠 뒤 발급받는 "앱 비밀번호" 16자리 (Gmail 로그인 비밀번호 아님)
# gemini.api.key와 같은 방식으로 환경변수에서 읽는다. 값을 여기 직접 적으면 GitHub에 그대로 올라간다.
# 환경변수가 없으면 메일을 보내지 않고 인증번호를 서버 로그에 출력한다.
mail.smtp.host=smtp.gmail.com
mail.smtp.port=587
mail.username=${MAIL_USERNAME:}
mail.password=${MAIL_PASSWORD:}